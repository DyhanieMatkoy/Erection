# 🔧 Решение проблемы QRadioButton: wrapped C/C++ object has been deleted

## 🚨 Проблема:
**Ошибка**: `Не удалось загрузить настройки: wrapped C/C++ object of type QRadioButton has been deleted`  
**Причина**: Вызов `load_settings()` до создания UI элементов (radio buttons)

---

## ✅ Корневой анализ проблемы:

### **Исходный код (проблемный):**
```python
def __init__(self, parent=None):
    super().__init__(parent)
    self.config = configparser.ConfigParser()
    self.config_file = 'env.ini'
    self.init_ui()              # Создает UI включая create_interface_tab()
    self.load_settings()          # ❌ Вызывается слишком рано
```

### **Последовательность вызовов:**
1. `__init__()` → `init_ui()` → `create_interface_tab()` (строка 41)
2. `create_interface_tab()` создает radio buttons (строки 170-200)
3. `load_settings()` вызывался на строке 19 (ДО создания radio buttons!)

### **Проблема в деталях:**
- `load_settings()` пытался установить `setChecked(True)` для несуществующих radio buttons
- Qt возвращал ошибку доступа к удаленному C++ объекту
- Radio buttons ещё не были созданы в момент загрузки настроек

---

## 🔧 Решение проблемы:

### **1. Отложенная загрузка настроек:**
```python
def __init__(self, parent=None):
    super().__init__(parent)
    self.config = configparser.ConfigParser()
    self.config_file = 'env.ini'
    self.init_ui()
    # ❌ БЫЛО: self.load_settings()
    # ✅ СТАЛО: Отложенный вызов после создания UI
    QTimer.singleShot(0, self.load_settings)
```

### **2. Добавление импорта:**
```python
from PyQt6.QtCore import QTimer  # Новый импорт
```

### **3. Удаление избыточных проверок:**
```python
# ❌ БЫЛО (проверки избыточны после исправления):
if button_style == 'text' and hasattr(self, 'use_text_icons_checkbox'):
    self.use_text_icons_checkbox.setChecked(True)

# ✅ СТАЛО (radio buttons гарантированно существуют):
if button_style == 'text':
    self.use_text_icons_checkbox.setChecked(True)
```

---

## 🧪 Тестирование решения:

### **Созданы тесты:**
1. **`test_settings_dialog.py`** - Комплексное тестирование
2. **`test_simple_settings.py`** - Простой тест импорта и создания
3. **`test_final_settings.py`** - Финальная проверка исправления

### **Тестовые сценарии:**
- ✅ Создание SettingsDialog без ошибок
- ✅ Загрузка настроек с существующим env.ini
- ✅ Загрузка настроек без файла (значения по умолчанию)
- ✅ Сохранение настроек и проверка файла конфигурации

---

## 📁 Измененные файлы:

### **`src/views/settings_dialog.py`:**
```python
# 1. Отложенная загрузка настроек
QTimer.singleShot(0, self.load_settings)

# 2. Удален hasattr() для radio buttons (больше не нужны)
if button_style == 'text':
    self.use_text_icons_checkbox.setChecked(True)

# 3. Добавлен импорт QTimer
from PyQt6.QtCore import QTimer
```

---

## 🎯 Как это работает:

### **Правильная последовательность событий:**
1. **`__init__()`** → `init_ui()` → создание всех вкладок
2. **`create_interface_tab()`** → создание всех radio buttons
3. **`QTimer.singleShot(0, ...)`** → планирование вызова `load_settings()` 
4. **Цикл событий Qt** → `load_settings()` вызывается ПОСЛЕ создания UI
5. **`load_settings()`** → безопасная установка состояний radio buttons

### **Преимущества решения:**
- ✅ **Безопасно**: Нет доступа к несуществующим объектам
- ✅ **Правильно**: Сохранен правильный порядок инициализации
- ✅ **Надежно**: Работает с любым конфигурационным файлом
- ✅ **Обратно совместимо**: Не меняет интерфейс SettingsDialog

---

## ✅ Результат:

**Проблема полностью решена!**
- ✅ Ошибка `wrapped C/C++ object has been deleted` устранена
- ✅ Настройки загружаются корректно при любом сценарии
- ✅ Radio buttons устанавливаются в правильные состояния
- ✅ Сохранение настроек работает без ошибок

**SettingsDialog теперь работает стабильно!** 🚀