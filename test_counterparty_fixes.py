"""Тест исправлений Counterparty"""
import sys
import os
sys.path.append(os.path.join(os.path.dirname(__file__), 'src'))

from PyQt6.QtWidgets import QApplication
from PyQt6.QtCore import Qt
from src.views.counterparty_form import CounterpartyForm

def test_counterparty_create():
    """Тест создания контрагента с UUID"""
    app = QApplication(sys.argv)
    
    try:
        # Создание новой формы
        form = CounterpartyForm(0)
        form.show()
        
        print("✅ Форма контрагента создана успешно")
        print("🧪 Проверьте:")
        print("   1. Введите название и сохраните")
        print("   2. Убедитесь что нет ошибки UUID")
        print("   3. Проверьте что запись создалась в БД")
        
        return app.exec()
    except Exception as e:
        print(f"❌ Ошибка создания формы: {e}")
        return 0

def test_copy_functionality():
    """Инструкции для тестирования копирования"""
    print("🧪 Тестирование копирования (F9):")
    print("   1. Откройте список контрагентов")
    print("   2. Выберите любой контрагент")
    print("   3. Нажмите F9 или кнопку копирования")
    print("   4. Проверьте что открылась форма с заполненными полями")
    print("   5. Название должно быть 'Копия - [оригинальное название]'")
    print("   6. Все поля должны быть заполнены данными выбранного контрагента")
    print("   7. UUID должен генерироваться автоматически")

if __name__ == "__main__":
    print("🚀 Тест исправлений Counterparty")
    print("=" * 50)
    
    print("\n1. Тест создания контрагента:")
    print("   python test_counterparty_fixes.py create")
    
    print("\n2. Инструкции для тестирования копирования:")
    test_copy_functionality()
    
    if len(sys.argv) > 1 and sys.argv[1] == 'create':
        test_counterparty_create()