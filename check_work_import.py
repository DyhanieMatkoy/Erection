"""Инструкции по проверке импорта работ"""
import sys
import os
sys.path.append(os.path.join(os.path.dirname(__file__), 'src'))

from PyQt6.QtWidgets import QApplication, QMessageBox

def show_import_instructions():
    """Показать инструкции по проверке импорта работ"""
    
    print("🔍 Проверка импорта работ из DBF")
    print("=" * 60)
    
    print("\n📋 Проблема: Работы импортируются успешно, но не видны в списке")
    
    print("\n🔧 Причина:")
    print("   • Список работ по умолчанию показывает только корневые элементы (parent_id = None)")
    print("   • Импортированные работы могут иметь parent_id > 0 (дочерние элементы)")
    print("   • Поэтому они не отображаются в корневом списке")
    
    print("\n🎯 Решение:")
    print("   1. Откройте список работ")
    print("   2. Используйте навигацию для поиска дочерних элементов:")
    print("      - Нажмите на группы работ (is_group = True)")
    print("      - Используйте кнопку '⬆' для навигации вверх")
    print("      - Ищите работы с конкретными именами из DBF")
    
    print("\n🧪 Проверка импорта:")
    print("   1. Запустите DBF Importer")
    print("   2. Выберите папку с DBF файлами")
    print("   3. Нажмите 'Импортировать все данные'")
    print("   4. После успешного импорта откройте список работ")
    print("   5. Ищите импортированные элементы:")
    
    print("\n   💡 Пример поиска:")
    print("      - Если в DBF была работа 'Бетонные работы'")
    print("      - Ищите ее в списке работ")
    print("      - Проверяйте группы работ")
    
    print("\n📊 Проверка в базе данных:")
    print("   SELECT id, name, parent_id, is_group FROM works WHERE marked_for_deletion = 0;")
    
    print("\n⚠️ Возможные проблемы:")
    print("   1. marked_for_deletion = 1 (работы помечены как удаленные)")
    print("   2. parent_id указывает на несуществующую группу")
    print("   3. Проблемы с кодировкой имен из DBF")
    
    print("\n🔧 Дополнительная проверка:")
    print("   Если работы не видны нигде, проверьте:")
    print("   - Откройте навигацию по группам работ")
    print("   - Проверьте что works импортировались в базу данных")
    print("   - Проверьте логи DBF Importer на ошибки")

if __name__ == "__main__":
    app = QApplication(sys.argv)
    
    reply = QMessageBox.question(
        None,
        "Проверка импорта работ",
        "Показать инструкции по поиску импортированных работ из DBF?",
        QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No,
        QMessageBox.StandardButton.Yes
    )
    
    if reply == QMessageBox.StandardButton.Yes:
        show_import_instructions()
        input("\n\nНажмите Enter для выхода...")