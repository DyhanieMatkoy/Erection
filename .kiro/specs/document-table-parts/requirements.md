# Требования для табличных частей документов

## Введение

Табличные части документов являются ключевым элементом пользовательского интерфейса в системе, обеспечивающим эффективное отображение, навигацию и управление строками данных внутри документов. Данная спецификация определяет требования к проектированию и реализации табличных частей документов для обеспечения единообразного пользовательского опыта и высокой производительности.

## Глоссарий

- **Табличная_Часть**: Табличный раздел документа или формы, содержащий строки с данными
- **Панель_Управления_Строками**: Панель инструментов с кнопками для управления строками табличной части
- **Командная_Панель**: Панель с кнопками команд, расположенная над табличной частью
- **Строка_Документа**: Отдельная запись в табличной части документа
- **Форма_Списка_v2**: Обновленная версия форм списков с улучшенными шорткатами и настройками иконок
- **Навигация_По_Иерархии**: Система навигации по иерархическим структурам данных

## Требования

### Требование 1: Панель управления строками

**Пользовательская история:** Как пользователь, я хочу иметь панель с кнопками управления строками над табличной частью документа, чтобы эффективно управлять данными документа.

#### Критерии приемки

1. КОГДА пользователь открывает форму документа с табличной частью ТО Система ДОЛЖНА отобразить панель управления строками над табличной частью
2. КОГДА панель управления строками отображается ТО Система ДОЛЖНА включить следующие кнопки: "Добавить", "Удалить", "Переместить выше", "Переместить ниже", "Импорт", "Экспорт", "Печать"
3. КОГДА пользователь наводит курсор на кнопку панели управления ТО Система ДОЛЖНА отобразить всплывающую подсказку с описанием действия
4. КОГДА строки в табличной части не выбраны ТО Система ДОЛЖНА отключить кнопки, требующие выбора строк ("Удалить", "Переместить выше", "Переместить ниже")
5. КОГДА выбрана первая строка ТО Система ДОЛЖНА отключить кнопку "Переместить выше"
6. КОГДА выбрана последняя строка ТО Система ДОЛЖНА отключить кнопку "Переместить ниже"

### Требование 2: Интеграция существующих команд формы

**Пользовательская история:** Как пользователь, я хочу, чтобы существующие команды формы, касающиеся строк документа, вызывались из командной панели табличной части, чтобы иметь единый интерфейс управления.

#### Критерии приемки

1. КОГДА в форме документа существуют команды для работы со строками ТО Система ДОЛЖНА интегрировать их в панель управления строками табличной части
2. КОГДА пользователь нажимает кнопку "Добавить" в панели управления ТО Система ДОЛЖНА вызвать соответствующую команду формы для добавления строки
3. КОГДА пользователь нажимает кнопку "Удалить" в панели управления ТО Система ДОЛЖНА вызвать соответствующую команду формы для удаления выбранных строк
4. КОГДА команды формы недоступны для текущего контекста ТО Система ДОЛЖНА отключить соответствующие кнопки в панели управления
5. КОГДА команда формы выполняется успешно ТО Система ДОЛЖНА обновить состояние панели управления в соответствии с новым состоянием табличной части

### Требование 3: Шорткаты и настройки иконок по аналогии с формами списков v2

**Пользовательская история:** Как пользователь, я хочу использовать те же горячие клавиши и видеть те же иконки в табличных частях, что и в формах списков v2, чтобы иметь единообразный интерфейс.

#### Критерии приемки

1. КОГДА пользователь работает с табличной частью документа ТО Система ДОЛЖНА поддерживать горячие клавиши: Insert (добавить строку), Delete (удалить строку), F4 (открыть форму выбора для поля справочника), Ctrl+C/Ctrl+V (копировать/вставить строки), Ctrl+Plus/Ctrl+Minus (добавить/удалить строку)
2. КОГДА пользователь работает с табличной частью документа ТО Система ДОЛЖНА поддерживать горячие клавиши перемещения строк: Ctrl+Shift+Up (переместить строку вверх), Ctrl+Shift+Down (переместить строку вниз)
3. КОГДА отображаются кнопки панели управления строками ТО Система ДОЛЖНА использовать стандартные иконки: "+" для добавления, "🗑" для удаления, "↑" для перемещения вверх, "↓" для перемещения вниз, "📥" для импорта, "📤" для экспорта, "🖨" для печати
4. КОГДА пользователь настраивает отображение панели управления ТО Система ДОЛЖНА сохранить настройки в пользовательских предпочтениях
5. КОГДА пользователь открывает форму документа повторно ТО Система ДОЛЖНА применить сохраненные настройки панели управления

### Требование 4: Возврат шорткатов навигации по иерархии в списки v2

**Пользовательская история:** Как пользователь, я хочу использовать шорткаты навигации по иерархии из v1 в обновленных формах списков v2, чтобы эффективно перемещаться по иерархическим структурам.

#### Критерии приемки

1. КОГДА пользователь работает с формой списка v2 с иерархической структурой ТО Система ДОЛЖНА поддерживать горячие клавиши навигации: Ctrl+→ (развернуть узел), Ctrl+← (свернуть узел), Ctrl+Shift+→ (развернуть все дочерние узлы), Ctrl+Shift+← (свернуть все дочерние узлы)
2. КОГДА пользователь нажимает Home ТО Система ДОЛЖНА перейти к первому элементу списка
3. КОГДА пользователь нажимает End ТО Система ДОЛЖНА перейти к последнему элементу списка
4. КОГДА пользователь нажимает Ctrl+Home ТО Система ДОЛЖНА перейти к корневому элементу иерархии
5. КОГДА пользователь нажимает Ctrl+End ТО Система ДОЛЖНА перейти к последнему элементу в развернутой иерархии
6. КОГДА пользователь нажимает Page Up/Page Down ТО Система ДОЛЖНА прокручивать список на одну страницу вверх/вниз с сохранением выбора

### Требование 5: Функциональность импорта/экспорта

**Пользовательская история:** Как пользователь, я хочу импортировать и экспортировать данные табличной части, чтобы эффективно работать с внешними источниками данных.

#### Критерии приемки

1. КОГДА пользователь нажимает кнопку "Импорт" ТО Система ДОЛЖНА открыть диалог выбора файла с поддержкой форматов Excel (.xlsx, .xls) и CSV
2. КОГДА пользователь выбирает файл для импорта ТО Система ДОЛЖНА отобразить предварительный просмотр данных с возможностью сопоставления колонок
3. КОГДА пользователь подтверждает импорт ТО Система ДОЛЖНА добавить данные в табличную часть с проверкой корректности и отображением результатов импорта
4. КОГДА пользователь нажимает кнопку "Экспорт" ТО Система ДОЛЖНА предложить выбор формата (Excel, CSV) и экспортировать текущие данные табличной части
5. КОГДА экспорт завершен ТО Система ДОЛЖНА предложить открыть или сохранить созданный файл

### Требование 6: Функциональность печати

**Пользовательская история:** Как пользователь, я хочу печатать данные табличной части документа, чтобы получать бумажные копии для работы.

#### Критерии приемки

1. КОГДА пользователь нажимает кнопку "Печать" ТО Система ДОЛЖНА открыть диалог настройки печати с предварительным просмотром
2. КОГДА отображается предварительный просмотр ТО Система ДОЛЖНА показать табличную часть в формате, оптимизированном для печати
3. КОГДА пользователь настраивает параметры печати ТО Система ДОЛЖНА предоставить опции: ориентация страницы, масштаб, поля, заголовки колонок на каждой странице
4. КОГДА пользователь подтверждает печать ТО Система ДОЛЖНА отправить документ на выбранный принтер с применением настроек
5. КОГДА табличная часть содержит много строк ТО Система ДОЛЖНА автоматически разбивать данные на страницы с повторением заголовков

### Требование 7: Перемещение строк

**Пользовательская история:** Как пользователь, я хочу изменять порядок строк в табличной части документа, чтобы организовать данные в нужной последовательности.

#### Критерии приемки

1. КОГДА пользователь выбирает одну или несколько строк и нажимает "Переместить выше" ТО Система ДОЛЖНА переместить выбранные строки на одну позицию вверх
2. КОГДА пользователь выбирает одну или несколько строк и нажимает "Переместить ниже" ТО Система ДОЛЖНА переместить выбранные строки на одну позицию вниз
3. КОГДА пользователь нажимает Ctrl+Shift+Up в табличной части ТО Система ДОЛЖНА переместить выбранные строки на одну позицию вверх
4. КОГДА пользователь нажимает Ctrl+Shift+Down в табличной части ТО Система ДОЛЖНА переместить выбранные строки на одну позицию вниз
5. КОГДА пользователь перемещает строки ТО Система ДОЛЖНА сохранить выбор на перемещенных строках
6. КОГДА перемещение строк выполнено ТО Система ДОЛЖНА автоматически пересчитать итоговые суммы документа за время не более 200 миллисекунд
7. КОГДА пользователь использует drag-and-drop для перемещения строк ТО Система ДОЛЖНА поддерживать визуальную индикацию места вставки

### Требование 8: Автоматические пересчеты

**Пользовательская история:** Как пользователь, я хочу, чтобы система автоматически пересчитывала суммы при изменении данных в табличной части, чтобы всегда видеть актуальные итоги.

#### Критерии приемки

1. КОГДА пользователь изменяет значение поля Количество в табличной части ТО Система ДОЛЖНА автоматически пересчитать поле Сумма как произведение Количество × Цена за время не более 100 миллисекунд
2. КОГДА пользователь изменяет значение поля Цена в табличной части ТО Система ДОЛЖНА автоматически пересчитать поле Сумма как произведение Количество × Цена за время не более 100 миллисекунд
3. КОГДА происходит изменение любой строки табличной части ТО Система ДОЛЖНА автоматически пересчитать итоговые суммы документа за время не более 200 миллисекунд
4. КОГДА пользователь добавляет или удаляет строку в табличной части ТО Система ДОЛЖНА автоматически пересчитать итоговые суммы документа за время не более 200 миллисекунд
5. КОГДА выполняется пересчет ТО Система ДОЛЖНА отобразить индикатор обновления данных

### Требование 9: Настройка отображения панели управления

**Пользовательская история:** Как пользователь, я хочу настраивать отображение кнопок в панели управления строками, чтобы адаптировать интерфейс под свои потребности.

#### Критерии приемки

1. КОГДА пользователь щелкает правой кнопкой мыши по панели управления строками ТО Система ДОЛЖНА отобразить контекстное меню с опцией "Настроить панель"
2. КОГДА пользователь выбирает "Настроить панель" ТО Система ДОЛЖНА открыть диалог настройки с деревом доступных команд
3. КОГДА пользователь изменяет видимость команд ТО Система ДОЛЖНА немедленно применить изменения к панели управления
4. КОГДА пользователь скрывает команды ТО Система ДОЛЖНА переместить их в подменю "Еще"
5. КОГДА пользователь сохраняет настройки ТО Система ДОЛЖНА применить их ко всем табличным частям данного типа документа

### Требование 10: Интеграция с формами выбора

**Пользовательская история:** Как пользователь, я хочу использовать формы выбора для заполнения полей справочников в табличной части, чтобы быстро находить и выбирать нужные значения.

#### Критерии приемки

1. КОГДА пользователь нажимает F4 в поле справочника табличной части ТО Система ДОЛЖНА открыть форму выбора элемента справочника с возможностью быстрого поиска
2. КОГДА пользователь начинает вводить текст в поле справочника ТО Система ДОЛЖНА предоставить автодополнение с выпадающим списком подходящих элементов (минимум 3 символа для активации)
3. КОГДА открывается форма выбора ТО Система ДОЛЖНА автоматически позиционировать курсор на текущем выбранном значении
4. КОГДА пользователь выбирает элемент из формы выбора ТО Система ДОЛЖНА заполнить связанные поля в строке табличной части (например, цену, единицу измерения)
5. КОГДА форма выбора используется в табличной части документов ТО Система ДОЛЖНА поддерживать навигацию по иерархии с теми же шорткатами, что и в списках v2

### Требование 11: Компактные кнопки для ссылочных типов

**Пользовательская история:** Как пользователь, я хочу иметь компактные кнопки открытия и выбора для ссылочных полей, чтобы эффективно использовать пространство интерфейса.

#### Критерии приемки

1. КОГДА отображается поле ввода ссылочного типа ТО Система ДОЛЖНА разместить компактную кнопку открытия с подписью 'o' внутри поля ввода
2. КОГДА отображается поле ввода ссылочного типа ТО Система ДОЛЖНА разместить компактную кнопку селектора внутри поля ввода, а не через отступ от него
3. КОГДА пользователь нажимает кнопку открытия 'o' ТО Система ДОЛЖНА открыть форму элемента справочника для просмотра или редактирования
4. КОГДА пользователь нажимает кнопку селектора ТО Система ДОЛЖНА открыть форму выбора элемента справочника
5. КОГДА кнопки размещены внутри поля ввода ТО Система ДОЛЖНА обеспечить достаточное пространство для отображения текста значения

### Требование 12: Компоновка полей в форме документа

**Пользовательская история:** Как пользователь, я хочу видеть поля формы документа в удобной компоновке, чтобы эффективно работать с данными.

#### Критерии приемки

1. КОГДА форма документа содержит 6 и более полей ввода в шапке ТО Система ДОЛЖНА верстать их в 2 колонки
2. КОГДА поля размещаются в 2 колонки ТО Система ДОЛЖНА учитывать тип редактируемых данных для оптимального размещения
3. КОГДА поле содержит длинные строки ТО Система ДОЛЖНА оставлять такие поля в 1 колонку на всю ширину формы
4. КОГДА поля размещены в 2 колонки ТО Система ДОЛЖНА обеспечить равномерное распределение полей между колонками
5. КОГДА изменяется размер окна формы ТО Система ДОЛЖНА адаптировать компоновку полей для оптимального использования пространства