# Desktop Work Composition Enhancements

## Реализованные улучшения

### 1. Контекстное меню для статей затрат

Добавлено контекстное меню для таблицы статей затрат с функциями:
- **Добавить статью затрат** - открывает диалог выбора статьи затрат
- **Удалить статью затрат** - удаляет выбранную статью затрат (с проверкой на наличие материалов)

**Файлы изменены:**
- `src/views/widgets/cost_items_table.py` - добавлено контекстное меню
- `src/views/work_form.py` - подключены сигналы контекстного меню

### 2. Контекстное меню для материалов

Добавлено контекстное меню для таблицы материалов с функциями:
- **Добавить материал** - открывает многошаговый диалог добавления материала
- **Удалить материал** - удаляет выбранный материал

**Файлы изменены:**
- `src/views/widgets/materials_table.py` - добавлено контекстное меню
- `src/views/work_form.py` - подключены сигналы контекстного меню

### 3. Иконка группы в списке работ

Иконка группы (📁) уже реализована в списке работ:
- Группы работ отображаются с иконкой папки перед названием
- Реализована в `src/views/work_list_form.py`

## Функциональность

### Редактирование количества композиции

✅ **Реализовано**: Пользователи могут редактировать количество материалов и статей затрат:
- Двойной клик или прямое редактирование в ячейке "Количество"
- Автоматический пересчет общей стоимости
- Валидация (количество > 0)

### Автоматический пересчет сумм

✅ **Реализовано**: Система автоматически пересчитывает:
- Сумму по каждому материалу (цена × количество)
- Общую стоимость работы (статьи затрат + материалы)
- Обновление происходит в реальном времени при изменении количества

### Контекстное меню для управления композицией

✅ **Реализовано**: Правый клик на таблицах открывает контекстное меню:
- **Статьи затрат**: Добавить/Удалить статью затрат
- **Материалы**: Добавить/Удалить материал
- Иконки для действий (файл для добавления, корзина для удаления)

## Использование

### Добавление статьи затрат
1. Правый клик на таблице статей затрат
2. Выбрать "Добавить статью затрат"
3. В диалоге выбрать нужную статью затрат
4. Статья добавляется в композицию работы

### Добавление материала
1. Правый клик на таблице материалов
2. Выбрать "Добавить материал"
3. Выбрать статью затрат (или добавить новую)
4. Выбрать материал
5. Указать количество на единицу работы

### Редактирование количества
1. Кликнуть на ячейку "Количество"
2. Ввести новое значение
3. Нажать Enter или кликнуть вне ячейки
4. Сумма пересчитается автоматически

### Удаление элементов
1. Выбрать строку в таблице
2. Правый клик → "Удалить"
3. Подтвердить удаление
4. Для статей затрат: проверяется наличие связанных материалов

## Валидация

- **Количество материалов**: должно быть > 0
- **Удаление статей затрат**: запрещено при наличии связанных материалов
- **Дублирование**: предотвращается добавление одинаковых статей затрат или материалов

## Технические детали

### Сигналы PyQt6
- `addCostItemRequested` - запрос на добавление статьи затрат
- `deleteCostItemRequested` - запрос на удаление статьи затрат  
- `addMaterialRequested` - запрос на добавление материала
- `deleteMaterialRequested` - запрос на удаление материала
- `quantityChanged` - изменение количества

### Контекстное меню
- Использует `QMenu` и `QAction`
- Стандартные иконки Qt для действий
- Условное отображение пунктов меню (удаление только при выборе строки)

Все требования по редактированию цен композиции с пересчетом сумм и контекстному меню для статей затрат выполнены в десктопной версии.