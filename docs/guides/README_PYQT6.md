# Система управления рабочим временем строительных бригад (PyQt6)

PyQt6 приложение для управления строительными сметами, учета трудозатрат и формирования отчетности.

## Требования

- Python 3.10 или выше
- pip

## Важно

Используется PyQt6 версии 6.7.1 (не 6.10.0) из-за проблем совместимости с DLL на Windows.

## Установка

### Автоматическая установка (Windows)

```bash
setup.bat
```

### Ручная установка

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Запуск

### Windows

```bash
run.bat
```

### Linux/Mac

```bash
python main.py
```

## Структура проекта

```
.
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── test_data.sql          # Тестовые данные
└── src/
    ├── data/              # Слой данных
    │   ├── database_manager.py
    │   ├── models/        # Модели данных
    │   └── repositories/  # Репозитории
    ├── services/          # Бизнес-логика
    ├── viewmodels/        # ViewModels
    └── views/             # UI формы
```

## Инициализация базы данных

При первом запуске база данных создастся автоматически. Для загрузки тестовых данных:

```bash
.\venv\Scripts\python.exe load_test_data.py
```

Или используйте любой SQLite клиент (DB Browser for SQLite, DBeaver и т.д.)

## Проверка статуса

Для проверки статуса проекта:

```bash
.\venv\Scripts\python.exe check_status.py
```

## Тестовые пользователи

- **admin** / **admin** - Администратор
- **manager** / **manager** - Руководитель
- **foreman** / **foreman** - Бригадир

## Функциональность

- ✅ Аутентификация и авторизация
- ✅ Управление базой данных SQLite
- ✅ CRUD операции для всех сущностей
- ✅ Автоматические расчеты в документах
- ✅ Горячие клавиши в стиле 1С 8.3
- ✅ Базовые UI формы
- 🔲 Формы справочников
- 🔲 Формы документов
- 🔲 Импорт из Excel
- 🔲 Печатные формы
- 🔲 Аналитические отчеты

## Горячие клавиши

### Формы списков
- **Insert** / **F9** - Создать новый элемент
- **Enter** - Открыть выбранный элемент
- **Delete** - Пометить на удаление
- **Ctrl+F** - Быстрый поиск
- **Ctrl+D** - Копировать элемент

### Формы документов
- **Ctrl+S** - Сохранить
- **Ctrl+Shift+S** - Сохранить и закрыть
- **Esc** - Закрыть (с подтверждением)
- **Ctrl+P** - Печать

### Табличные части
- **Insert** - Добавить строку
- **Delete** - Удалить строку
- **F4** - Выбор из справочника

## Технологии

- **PyQt6** - GUI фреймворк
- **SQLite3** - База данных
- **openpyxl** - Работа с Excel
- **reportlab** - Генерация PDF

## Архитектура

Проект использует MVVM (Model-View-ViewModel) архитектуру:

- **Models** - Модели данных (dataclasses)
- **Repositories** - Доступ к данным
- **Services** - Бизнес-логика
- **ViewModels** - Логика представления
- **Views** - UI формы (PyQt6 Widgets)

## Разработка

### Добавление новой формы

1. Создать модель в `src/data/models/`
2. Создать репозиторий в `src/data/repositories/`
3. Создать сервис в `src/services/`
4. Создать ViewModel в `src/viewmodels/`
5. Создать форму в `src/views/`

### Запуск в режиме разработки

```bash
python main.py
```

## Лицензия

Proprietary
