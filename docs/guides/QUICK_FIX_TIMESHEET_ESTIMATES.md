# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ: Сметы не загружаются в веб-версии

## Проблема
Веб-версия не показывает сметы в форме табеля. Ошибка 422 в консоли браузера.

## Причина
API ограничивает `page_size` до 100, а фронтенд запрашивает 10000.

## Решение (УЖЕ ПРИМЕНЕНО ✅)

Файл `web-client/src/views/documents/TimesheetFormView.vue` изменен:
- Добавлена пагинация для загрузки всех смет
- Сметы загружаются порциями по 100

## Что делать дальше

### 1. Пересобрать веб-клиент
```bash
cd web-client
npm run build
```

### 2. Перезапустить сервер (если нужно)
```bash
# Если используете dev сервер
npm run dev

# Или перезапустите production сервер
```

### 3. Проверить в браузере
1. Открыть http://localhost:5173 (или ваш URL)
2. Войти как admin
3. Перейти в "Документы → Табели"
4. Нажать "Создать"
5. Выбрать объект
6. Проверить, что сметы загружаются

### 4. Проверить консоль браузера (F12)
Должно быть:
```
✅ GET /api/documents/estimates?page=1&page_size=100 → 200 OK
✅ Loaded 8 estimates
```

НЕ должно быть:
```
❌ GET /api/documents/estimates?page=1&page_size=10000 → 422
❌ Failed to load estimates
```

## Если не помогло

1. Очистить кэш браузера (Ctrl+Shift+Delete)
2. Перезагрузить страницу (Ctrl+F5)
3. Проверить, что изменения применились:
   - Открыть DevTools (F12)
   - Вкладка Sources
   - Найти TimesheetFormView.vue
   - Проверить, что есть код с пагинацией

## Подробная документация

- **TIMESHEET_ESTIMATE_FILTER_CRITICAL_FIX.md** - Полное описание проблемы
- **TIMESHEET_ESTIMATE_FILTER_INDEX.md** - Навигация по всем документам

---

**Статус:** ✅ Исправлено
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
**Дата:** 2025-11-30
