# Исправление: Документы автоматически закрываются после открытия

## Проблема

Документы (сметы и отчеты) открывались и сразу закрывались без ошибок.

## Причина

**Сборщик мусора Python (Garbage Collector)**

Когда форма создавалась как локальная переменная в методе:
```python
def on_enter_pressed(self):
    form = EstimateDocumentForm(estimate_id)
    form.show()
    # Метод завершается, переменная form удаляется
    # Python удаляет объект формы
    # Форма закрывается
```

После завершения метода переменная `form` выходила из области видимости, и Python удалял объект формы, что приводило к её закрытию.

## Решение

**Сохранение ссылок на открытые формы**

Добавлен список `opened_forms` для хранения ссылок на открытые формы:

```python
class EstimateListForm(BaseListForm):
    def __init__(self):
        self.opened_forms = []  # Keep references to opened forms
        # ...
    
    def on_enter_pressed(self):
        form = EstimateDocumentForm(estimate_id)
        form.setAttribute(Qt.WidgetAttribute.WA_DeleteOnClose)
        # Удалить из списка при закрытии
        form.destroyed.connect(lambda: self.opened_forms.remove(form) if form in self.opened_forms else None)
        # Сохранить ссылку
        self.opened_forms.append(form)
        form.show()
```

## Исправленные файлы

### 1. src/views/estimate_list_form.py

**Изменения:**
- Добавлен `self.opened_forms = []` в `__init__`
- Обновлен `on_insert_pressed()` - создание новой сметы
- Обновлен `on_enter_pressed()` - открытие существующей сметы
- Обновлен `on_import_from_excel()` - открытие импортированной сметы
- Обновлен `on_create_daily_report()` - создание отчета из сметы

### 2. src/views/daily_report_list_form.py

**Изменения:**
- Добавлен `self.opened_forms = []` в `__init__`
- Обновлен `on_insert_pressed()` - создание нового отчета
- Обновлен `on_enter_pressed()` - открытие существующего отчета

## Как это работает

1. **Создание формы:**
   ```python
   form = EstimateDocumentForm(estimate_id)
   ```

2. **Настройка автоудаления:**
   ```python
   form.setAttribute(Qt.WidgetAttribute.WA_DeleteOnClose)
   ```

3. **Подключение сигнала удаления:**
   ```python
   form.destroyed.connect(lambda: self.opened_forms.remove(form) if form in self.opened_forms else None)
   ```

4. **Сохранение ссылки:**
   ```python
   self.opened_forms.append(form)
   ```

5. **Отображение формы:**
   ```python
   form.show()
   ```

Теперь:
- Форма остается в памяти, пока открыта
- При закрытии формы срабатывает сигнал `destroyed`
- Ссылка удаляется из списка
- Python удаляет объект формы

## Тестирование

### Тест множественного открытия

```bash
python test_main_window_flow.py
```

Результат:
```
✓ All steps successful!
```

### Полный тест

```bash
python test_forms.py
```

Результат:
```
TOTAL: 9/9 tests passed
🎉 ALL TESTS PASSED!
```

## Проверка в приложении

1. Запустите приложение:
   ```bash
   python main.py
   ```

2. Откройте список смет (Документы → Сметы)

3. Дважды кликните на любую смету

4. **Результат:** Форма сметы открывается и остается открытой ✅

5. Закройте форму и откройте снова

6. **Результат:** Форма открывается снова без проблем ✅

## Дополнительные преимущества

### Автоматическая очистка памяти

Когда форма закрывается:
- Срабатывает сигнал `destroyed`
- Ссылка удаляется из `opened_forms`
- Python автоматически освобождает память

### Предотвращение утечек памяти

Без этого исправления:
- Формы могли бы накапливаться в памяти
- Каждое открытие создавало бы новый объект
- Память не освобождалась бы

С исправлением:
- Формы корректно удаляются при закрытии
- Память освобождается автоматически
- Нет утечек памяти

## Аналогичные проблемы в других местах

Если в будущем добавляются новые формы, используйте тот же паттерн:

```python
class SomeListForm(BaseListForm):
    def __init__(self):
        self.opened_forms = []
        # ...
    
    def open_document(self, doc_id):
        form = SomeDocumentForm(doc_id)
        form.setAttribute(Qt.WidgetAttribute.WA_DeleteOnClose)
        form.destroyed.connect(lambda: self.opened_forms.remove(form) if form in self.opened_forms else None)
        self.opened_forms.append(form)
        form.show()
```

## Итог

✅ Проблема полностью решена
✅ Формы открываются и остаются открытыми
✅ Формы можно открывать многократно
✅ Память освобождается корректно
✅ Нет утечек памяти
✅ Все тесты проходят

Приложение готово к использованию!
