# Руководство по иерархическим справочникам

## Обзор изменений

Реализована поддержка иерархической навигации в справочниках и позиционирование курсора на текущем значении при выборе из формы.

## Изменения в базе данных

### Добавлено поле parent_id во все справочники:

- `works.parent_id` - ссылка на родительскую группу работ
- `counterparties.parent_id` - ссылка на родительскую группу контрагентов
- `objects.parent_id` - ссылка на родительскую группу объектов
- `organizations.parent_id` - ссылка на родительскую группу организаций
- `persons.parent_id` - ссылка на родительскую группу физических лиц

### Миграция

При запуске приложения автоматически добавляются поля `parent_id` в существующие таблицы справочников, если их еще нет.

## Функциональность

### 1. Иерархическая навигация в диалоге выбора

**ReferencePickerDialog** теперь поддерживает:

- **Навигацию по уровням иерархии**:
  - Кнопка "↑ Вверх" - переход на уровень выше
  - Backspace - быстрый переход вверх
  - Метка показывает текущую группу

- **Визуальное отображение групп**:
  - Элементы с дочерними элементами отмечены иконкой 📁
  - При поиске показываются все уровни

- **Навигация клавиатурой**:
  - Enter - открыть группу или выбрать элемент
  - Ctrl+Enter - выбрать элемент (даже если это группа)
  - Backspace - вернуться на уровень выше
  - Esc - отмена

- **Двойной клик**:
  - На группе - открывает группу
  - На элементе - выбирает элемент

### 2. Позиционирование курсора на текущем значении

При открытии диалога выбора из формы документа или справочника:

- Курсор автоматически позиционируется на текущем выбранном значении
- Если текущее значение находится в подгруппе, диалог открывается на корневом уровне, но курсор все равно позиционируется при поиске

**Примеры использования**:

```python
# В форме документа при выборе работы
dialog = ReferencePickerDialog("works", "Выбор работы", self, current_id=current_work_id)

# В форме документа при выборе заказчика
dialog = ReferencePickerDialog("counterparties", "Выбор заказчика", self, current_id=self.customer_id)

# В форме справочника при выборе родителя
dialog = ReferencePickerDialog("works", "Выбор родительской группы", self, current_id=self.parent_id)
```

### 3. Поддержка parent_id в формах справочников

Все формы справочников теперь имеют:

- Поле "Родитель" с кнопкой выбора (...)
- Кнопку очистки родителя (✕)
- Проверку на циклические ссылки (нельзя выбрать элемент родителем самого себя)

**Формы с поддержкой parent_id**:
- WorkForm
- CounterpartyForm
- ObjectForm
- OrganizationForm
- PersonForm

## Использование

### Создание иерархии справочников

1. Откройте форму справочника (например, "Работы")
2. Создайте новый элемент или откройте существующий
3. В поле "Родитель" нажмите кнопку "..." для выбора родительской группы
4. Выберите группу из диалога или оставьте пустым для корневого уровня
5. Сохраните элемент

### Навигация по иерархии при выборе

1. При выборе значения из формы документа откроется диалог выбора
2. Если текущее значение уже выбрано, курсор будет на нем
3. Используйте двойной клик или Enter для навигации по группам
4. Используйте кнопку "↑ Вверх" или Backspace для возврата
5. Используйте поиск для быстрого поиска по всем уровням

### Выбор из табличной части

При двойном клике на ячейке "Работа" в табличной части сметы:
1. Открывается диалог выбора работ
2. Курсор автоматически позиционируется на текущей работе (если она выбрана)
3. Можно навигировать по группам или использовать поиск
4. После выбора работы автоматически заполняются цена, единица измерения и норма трудозатрат

## Технические детали

### Структура ReferencePickerDialog

```python
class ReferencePickerDialog(QDialog):
    def __init__(self, table_name, title, parent=None, owner_id=None, current_id=None):
        # table_name - имя таблицы справочника
        # title - заголовок диалога
        # parent - родительское окно
        # owner_id - фильтр по владельцу (для objects)
        # current_id - текущее значение для позиционирования курсора
```

### Логика навигации

- При загрузке без поиска показываются только элементы текущего уровня
- При поиске показываются все элементы, соответствующие критерию
- Группы определяются наличием дочерних элементов
- Позиционирование курсора происходит после загрузки данных

### Проверка циклических ссылок

В формах справочников при выборе родителя проверяется:
```python
if self.work_id > 0 and selected_id == self.work_id:
    QMessageBox.warning(self, "Ошибка", "Нельзя выбрать элемент в качестве родителя самого себя")
    return
```

## Примеры сценариев использования

### Сценарий 1: Создание иерархии работ

1. Создайте группу "Строительные работы" (без родителя)
2. Создайте группу "Кровельные работы" с родителем "Строительные работы"
3. Создайте работу "Монтаж металлочерепицы" с родителем "Кровельные работы"

### Сценарий 2: Выбор работы в смете

1. Откройте смету
2. Добавьте строку в табличную часть
3. Дважды кликните на ячейке "Работа"
4. В диалоге выбора навигируйте: Строительные работы → Кровельные работы
5. Выберите "Монтаж металлочерепицы"

### Сценарий 3: Изменение работы в смете

1. Откройте смету с заполненными строками
2. Дважды кликните на ячейке с работой
3. Диалог откроется с курсором на текущей работе
4. Выберите другую работу или нажмите Esc для отмены

## Ограничения

- Глубина иерархии не ограничена, но рекомендуется не более 3-4 уровней для удобства
- При удалении группы с дочерними элементами нужно сначала переместить или удалить дочерние элементы
- Циклические ссылки проверяются только на прямое самоссылание (не проверяется цепочка A→B→C→A)

## Будущие улучшения

- Древовидное отображение в списках справочников
- Массовое перемещение элементов между группами
- Проверка полной цепочки циклических ссылок
- Drag & drop для перемещения элементов между группами
