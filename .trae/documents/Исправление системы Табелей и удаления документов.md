# 🎯 Финальный план: Исправление фильтрации и настройка кнопок

## 🚨 Основная задача: Проблема удаления помеченных документов
- **Причина**: `DataService.get_documents()` НЕ фильтрует `marked_for_deletion = False`
- **Решение**: Добавить фильтрацию в `DataService.get_documents()`

## 🔧 Новые требования по кнопкам:

### 1. Использовать значок ❌ для кнопки фильтрации "Очистить"
- **Место**: Все списковые формы с фильтрами
- **Иконка**: ❌ для кнопки "Очистить"
- **Соответствие**: В соответствии с настройками отображения заголовков кнопок

### 2. Добавить значки для кнопок в документе Смета
- **Аналогично**: Как в других документах
- **Принцип**: Выводить в соответствии с настройками кнопок
- **Значки**: Для всех кнопок, где их нет

### 3. Добавить настройку расположения кнопок
- **Новая настройка**: Расположение кнопок (наверху/внизу формы)
- **Кнопки для переноса**: Сохранить, Сохранить и закрыть, Закрыть, Провести, Отменить проведение, Печать
- **Перенос**: С нижней строки наверх форм

### 4. 🔥 НОВОЕ: Кнопка "Очистить" видна лишь при заполненном поле фильтра
- **Логика**: Показывать кнопку "Очистить" только когда есть что очищать
- **Условие**: `filter_field.text().strip() != ""`
- **Реализация**: Динамическое показ/скрытие кнопки
- **Применение**: Во всех фильтрах формы

## 🎯 Конкретный план исправлений:

### Этап 1: Исправление фильтрации удаленных документов
1. **Обновить `api/services/data_service.py`**:
   - Добавить фильтрацию по `marked_for_deletion = False` в `get_documents()`
   - Добавить опциональный параметр `include_deleted` для гибкости

### Этап 2: Реализация настроек кнопок
1. **Расширить настройки интерфейса**:
   - Добавить настройку "Расположение кнопок" (верх/низ)
   - Добавить настройку "Стиль кнопок" (текст/иконы/оба)

### Этап 3: Обновление иконок кнопок
1. **Добавить иконку ❌** для кнопки "Очистить":
   - Во всех фильтрах и списках
   - Через `ButtonStyler` или напрямую

2. **Добавить иконки в Смету**:
   - Проанализировать существующие кнопки
   - Добавить недостающие иконки
   - Привязать к настройкам отображения

### Этап 4: Перенос кнопок наверх
1. **Обновить формы документов**:
   - Добавить логику размещения кнопок вверху
   - Сохранить обратную совместимость
   - Учитывать настройку расположения

### Этап 5: 🔥 Реализация динамической кнопки "Очистить"
1. **Обновить все фильтры**:
   - Добавить `textChanged` сигнал для полей ввода
   - Показывать кнопку "Очистить" только при `text != ""`
   - Скрывать кнопку когда поле пустое

2. **Пример реализации**:
   ```python
   # В формах с фильтрами
   self.filter_input.textChanged.connect(self.update_clear_button_visibility)
   
   def update_clear_button_visibility(self):
       has_text = bool(self.filter_input.text().strip())
       self.clear_button.setVisible(has_text)
   ```

### Этап 6: Тестирование
1. **Проверить фильтрацию**: Удаленные документы не видны
2. **Проверить иконки**: Отображаются корректно
3. **Проверить расположение**: Кнопки вверху работают
4. **Проверить кнопку "Очистить"**: Видна только при заполненном поле

## 📁 Основные файлы для изменения:
- `api/services/data_service.py` (фильтрация удаленных)
- `src/views/utils/button_styler.py` (иконки кнопок)
- `src/views/estimate_document_form.py` (кнопки Сметы)
- Формы с фильтрами (кнопка "Очистить" с динамической видимостью)
- Настройки интерфейса (расположение кнопок)

## 🎯 Ожидаемый результат:
- ✅ Удаленные документы не отображаются в списках
- ✅ Кнопка "Очистить" имеет иконку ❌ и видна только при заполненном поле
- ✅ Смета имеет иконки на всех кнопках
- ✅ Кнопки можно расположить наверху или внизу
- ✅ Настройки учитываются во всех формах